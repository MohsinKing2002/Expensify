import { Text, View } from "react-native";

const Home = () => {
  return (
    <View className="flex h-full bg-gray-200 items-center justify-center">
      <Text className="text-red-700 text-xl font-bold">Home Page</Text>
    </View>
  );
};

export default Home;

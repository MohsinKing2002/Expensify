import { View, Text } from "react-native";

const Account = () => {
  return (
    <View className="flex h-full items-center justify-center">
      <Text className="text-red-700 text-xl font-bold">Account Page</Text>
    </View>
  );
};

export default Account;
